import 'package:firebase_auth/firebase_auth.dart';
import 'package:flutter/material.dart';

class Upload extends StatelessWidget {
  Upload(FirebaseUser user);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

}